# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# Data / DB
sqlmodel==0.0.22
pydantic==2.10.4
pydantic-settings==2.7.1

# Env
python-dotenv==1.0.1

# HTTP client for provider APIs
httpx==0.28.1

# Media placeholder generation (offline fallback for images)
Pillow==11.1.0
# Edge-guided smoothing for the 2.5D depth field.
scipy>=1.14,<2

# Optional provider SDKs (imported lazily; app runs without them).
# 0.116+ is required for the `output_config` structured-output parameter used
# by the script-generation adapter.
anthropic==0.116.0

# Animation engine for animation scenes: the script AI authors Manim code that
# is rendered in an isolated subprocess (backend/adapters/animation.py). OPTIONAL
# and heavy; without it, animation scenes fall back to a placeholder card.
# Requires Python 3.13-compatible Manim (0.19+). For typeset math (MathTex/Tex)
# install a LaTeX distribution (e.g. MiKTeX); Text-based scenes work without it.
manim>=0.19,<0.21

# Depth estimation for 2.5D parallax over still panels
# (backend/services/parallax.py). OPTIONAL, same contract as manim above: the
# renderer checks parallax.available() and falls back to a flat camera move when
# either the runtime or the weights are missing.
#   pip install onnxruntime
# Weights (Depth Anything V2 Small, ~99 MB) go in data/models/:
#   https://huggingface.co/onnx-community/depth-anything-v2-small/resolve/main/onnx/model.onnx
#   -> data/models/depth_anything_v2_small.onnx
# onnxruntime-gpu can replace onnxruntime; parallax.py picks CUDA when present.
onnxruntime; python_version >= "3.9"
